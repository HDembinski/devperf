[project]
name = "devperf"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["gitpython>=3.1.45", "rich>=14.2.0", "tabulate>=0.9.0"]

[project.scripts]
devperf = "main:main"
